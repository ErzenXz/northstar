{
  "name": "@origin/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --clean",
    "check": "tsc --noEmit",
    "dev": "tsx watch src/index.ts",
    "lint": "tsc --noEmit",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@octokit/rest": "22.0.0",
    "@origin/ai": "workspace:*",
    "@origin/core": "workspace:*",
    "@origin/db": "workspace:*",
    "@origin/git": "workspace:*",
    "dotenv": "17.2.2",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "4.20.5",
    "tsup": "8.5.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
